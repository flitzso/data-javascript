// Declaração de função simples
function saudacao() {
    console.log("Olá, mundo!");
  }
  
  // Declaração de função com parâmetros
  function somar(a, b) {
    return a + b;
  }
  
  // Declaração de função com retorno implícito (arrow function)
  const multiplicar = (a, b) => a * b;
  