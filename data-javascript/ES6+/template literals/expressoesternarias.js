const nota = 75;

// Uso de template literal com expressão ternária
const resultadoFinal = `O aluno está ${nota >= 60 ? 'aprovado' : 'reprovado'}.`;

console.log(resultadoFinal);
// Saída: O aluno está aprovado.
